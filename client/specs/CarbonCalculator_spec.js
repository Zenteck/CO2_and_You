const assert = require('assert');
const CarbonCalculator = require('./src/helpers/CarbonCalculator.js');

describe('CarbonCalculator', function {
  it('should be able get total score');

  const CarbonCalculator = new CarbonCalculator();

});
